/**
 * level between min and max of an arbitrary state (e.g. color saturation).
 *
 * 0.0 means the minimum of the field, 1.0 the maximum.
 *
 * can also represent relative "delta" values for change amount.
 */
export type Level = number
