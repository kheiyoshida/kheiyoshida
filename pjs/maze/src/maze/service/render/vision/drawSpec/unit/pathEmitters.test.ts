import { Layer } from '../../frame/layer'

describe(`emitters`, () => {
  it(`emitters should emit point from left to right`, () => {
    // Object.values(emitters).forEach(emit => {
    //   const res = emit(layer)
    //   expect(res.top[0][0]).toBeLessThan(res.top[1][0])
    //   expect(res.bottom[0][0]).toBeLessThan(res.bottom[1][0])
    // })
  })
})
