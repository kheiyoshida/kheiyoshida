import { wh, ww } from '../../../config'

export const btnSize = wh * 0.15
export const centerX = ww / 2
export const centerY = wh - wh * 0.08

// offset values
export const OffsetDelta = 20
export const RevertStateFrames = 3
export const MaxOffsetDelta = OffsetDelta * 2
